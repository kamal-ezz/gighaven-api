server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gighaven
    username: kamal
    password: password
    #driver-class-name:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  security:
    user:
      password: jjjj

app:
  jwtSecret: JWTSuperSecretKey
  jwtExpirationInMs: 604800000


gighaven:
  message_room:
    page_size: 500